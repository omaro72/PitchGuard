# Evaluations

This directory will contain versioned cases for live evaluation against local Ollama models, together with recorded results and the configuration needed to interpret them.

Evaluation cases must use fictional content. Recorded results must come from actual runs and must identify the model and relevant prompt version. No evaluation has been run and no results are included yet.
